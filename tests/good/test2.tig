/* arr1 is valid since expression 0 is int = myint */
let
	type myint = int
	type  arrtype = array of myint

	var arr1 : arrtype := arrtype [10] of 5
in
	if arr1[0] = 5 then print("Correcto") else print ("Incorrecto");arr1; 0
end
